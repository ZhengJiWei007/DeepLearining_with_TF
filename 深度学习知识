## 一.TensorFlow 程序通常分为两个阶段：
    1.构建计算图谱(构建阶段)：构建一个表示 ML 模型的计算图谱,然后对其进行训练,计算
    2.运行计算图谱(执行阶段):执行阶段通常运行循环，重复地求出训练步骤（例如，每个小批次），逐渐改进模型参数

## 二.单进程TensorFlow中，多个回话不会共享任何状态，即使她们共享同一个图(每个会话都有自己的每个变量的副本),在分布式 TensorFlow 中，变量状态存储在服务器上，而不是在会话中，因此多个会话可以共享相同的变量

## 三.TensorFlow 操作（也简称为 ops）


